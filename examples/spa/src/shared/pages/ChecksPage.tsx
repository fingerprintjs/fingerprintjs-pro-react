import UnmountedComponent from '../../special_checks/UnmountedComponent'

function ChecksPage() {
  return (
    <section className='body'>
      <h3>Special checks page</h3>
      <UnmountedComponent />
    </section>
  )
}

export default ChecksPage
